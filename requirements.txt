# ────────────────────────────────────
# MvpMonitoreo — Dependencias
# pip install -r requirements.txt
# ────────────────────────────────────

# Base de datos
asyncpg>=0.29.0     # PostgreSQL async (equivalente a PDO de Laravel)
python-dotenv>=1.0  # Leer .env (equivalente al bootstrap de Laravel)

# API Web
fastapi>=0.111.0    # Framework REST (equivalente a Laravel pero async)
uvicorn>=0.29.0     # Servidor ASGI (equivalente a php artisan serve)
pydantic>=2.0.0     # Validación de schemas (equivalente a Form Requests)

# Pruebas
pytest>=8.0.0
pytest-asyncio>=0.23.0


# Monitor de recursos del VPS
psutil>=5.9.0       # CPU, RAM en tiempo real

# SNMP — descomentar cuando el community string esté confirmado
# pysnmp>=6.0.0     # Lector SNMP para switches, routers, impresoras

# SSH — descomentar cuando credenciales SSH estén disponibles
# asyncssh>=2.14.0  # Lector SSH para servidores Linux
requests>=2.31.0    # Cliente HTTP simple
rich>=13.7.0        # Terminal interactiva, tablas, colores
PyJWT>=2.8.0        # Generación/validación de tokens JWT
passlib>=1.7.4
bcrypt==3.2.2
pandas>=2.0.0       # Lectura de Excel/CSV
websockets>=12.0    # Soporte para WebSockets
